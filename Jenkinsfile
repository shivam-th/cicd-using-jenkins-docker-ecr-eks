pipeline {
    agent any
      stages {
        stage('Helm Dsepdody') {
          steps {
            script {
              sh "kubectl get nodes"
            }
          }
        } 
        
    }
}
